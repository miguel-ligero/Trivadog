<form method="post" action="{{ path('tipohospedaje_delete', {'id': tipohospedaje.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tipohospedaje.id) }}">
    <button class="btn btn-primary mb-4 btn-home">Borrar</button>
</form>
